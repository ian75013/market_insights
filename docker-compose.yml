services:
  api:
    build: .
    container_name: market-insights-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn market_insights.api.main:app --host 0.0.0.0 --port 8000
